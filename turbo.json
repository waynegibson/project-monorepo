{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV"],
  "pipeline": {
    "dev": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".output/**", ".cdk.out/**"]
    },
    "db:generate": {},
    "db:migrate:deploy": {},
    "db:push": {}
  }
}
